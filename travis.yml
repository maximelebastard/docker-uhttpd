sudo: required

language: go

services:
  - docker

before_install:
  - go get github.com/nesv/uhttpd
  - docker build -t maximelebastard/alpine-uhttpd .

deploy:
  provider: script
  script: bash docker_push.sh
  on:
    branch: master



